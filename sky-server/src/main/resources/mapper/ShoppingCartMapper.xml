<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.ShoppingCartMapper">

    <!-- 根据用户id查询购物车数据-->
    <select id="list" resultType="com.sky.entity.ShoppingCart">
        select * from shopping_cart
        <where>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="dishId != null">
                and dish_id = #{dishId}
            </if>
            <if test="setmealId != null">
                and setmeal_id = #{setmealId}
            </if>
            <if test="dishFlavor != null">
                and dish_flavor = #{dishFlavor}
            </if>
        </where>
    </select>

    <!--    更新购物车商品数量-->
    <update id="updateNumberById">
        update shopping_cart
        set number = #{number}
        where id = #{id}
    </update>

    <!--    向购物车添加商品-->
    <insert id="insert">
        insert into shopping_cart (user_id, dish_id, setmeal_id, dish_flavor, number, create_time, amount, name, image)
        values (#{userId}, #{dishId}, #{setmealId}, #{dishFlavor}, #{number}, #{createTime}, #{amount}, #{name}, #{image})
    </insert>

    <!--    删除购物车数据-->
    <delete id="deleteByUserId">
        delete from shopping_cart
        where user_id = #{userId}
    </delete>

    <!-- 根据id删除购物车数据 -->
    <delete id="deleteById">
        delete from shopping_cart where id = #{id}
    </delete>

    <insert id="insertBatch">
        INSERT INTO shopping_cart (user_id, dish_id, setmeal_id, number, amount, name, image)
        VALUES
        <foreach collection="shoppingCartList" item="cart" separator=",">
            (#{cart.userId}, #{cart.dishId}, #{cart.setmealId}, #{cart.number}, #{cart.amount}, #{cart.name}, #{cart.image})
        </foreach>
    </insert>

</mapper>
